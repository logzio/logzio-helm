apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: appdetectors.logz.io
spec:
  group: logz.io
  names:
    kind: AppDetector
    listKind: AppDetectorList
    plural: appdetectors
    singular: appdetector
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: AppDetector is the Schema for the appdetectors
            API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: AppDetectorSpec defines the desired state of
                AppDetector
              properties:
                applications:
                  items:
                    properties:
                      containerName:
                        type: string
                      application:
                        type: string
                      processName:
                        type: string
                    type: object
                  type: array
                detected:
                  type: boolean
              type: object
            status:
              description: AppDetectorStatus defines the observed state
                of AppDetector
              properties:
                appDetection:
                  properties:
                    phase:
                      enum:
                        - Pending
                        - Running
                        - Completed
                        - Error
                      type: string
                  type: object
                detected:
                  type: boolean
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
